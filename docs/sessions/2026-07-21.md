# Session 2026-07-21

## Decisions Made
- `structure.md` added at project root as ChatGPT context file — not git-ignored (decision left to user)
- VS Code Python interpreter confirmed to use `venv/` (not `.venv/`) — no action needed

## Work Done
- Loaded and processed `docs/ChatGPT/injura_session_context_2026_06_19_phase_smoothing_windowing.md`
- Updated `CLAUDE.md` — session date, E2/E3-A complete, E3-B in progress, next steps updated
- Created `docs/sessions/2026-06-19.md` — backfilled session log from ChatGPT context
- Committed docs to `main`, merged `main` into `feature/biomechanics`
- Created `structure.md` at project root (renamed to `structure.md` by user)
- Started E3-B Core Metrics:
  - Designed `calculate_squat_depth()` — normalized depth ratio using hip_y + SquatPhaseThresholds
  - User implemented `engine/squat/metrics.py`:
    - `SquatDepthResult` dataclass — depth_ratio, deepest_index, deepest_hip_y, standing_y, bottom_y
    - `calculate_squat_depth(hip_y_values, rep, thresholds) -> SquatDepthResult`
    - Uses `max()` over rep window to find actual deepest frame (not just `rep.bottom_index`)

## Next Steps
- Tests for `calculate_squat_depth`
- Continue E3-B: torso lean, knee travel, heel lift, neutral spine proxy

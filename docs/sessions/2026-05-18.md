# Session 2026-05-18

## Decisions Made
- `.claude/commands/` setup dropped — not needed at current project scale
- `docs/ChatGPT/` added to `.gitignore` — external conversation logs not tracked in repo
- `visualize()` separated from `extract()` — data extraction and rendering are distinct concerns
- `waitKey` removed from `visualize()` — timing control delegated to the caller

## Work Done
- `engine/pose/types.py`: `Landmark` dataclass, `PoseFrame`, `PoseSequence` type aliases
- `engine/pose/base_estimator.py`: `BasePoseEstimator` abstract class with `extract()` and `close()`
- `engine/pose/mediapipe_estimator.py`: `MediaPipePoseEstimator` with:
  - `extract(video_path)` — reads video, runs MediaPipe, returns `PoseSequence`
  - `visualize(frame, pose_landmarks)` — draws landmarks on frame and displays with `imshow`
  - `close()` — releases MediaPipe resources
- `tests/test_pose_estimator.py`: two test functions:
  - `test_extract_pose_sequence` — runs extraction, prints frame and landmark counts
  - `test_visualize_pose_estimation` — runs visualization loop with `q` to quit

## Fixes Applied
- `video_pat` typo → `video_path` in `base_estimator.py`
- Removed debug `print` statements from `mediapipe_estimator.py`
- Removed duplicate `waitKey` from `visualize()`

## Next Steps
- Run `test_pose_estimator.py` and verify output
- Define `ExerciseAnalyzer` base class
- Implement `SquatAnalyzer`
- Create `squat_rules.yaml`

# Session 2026-06-06

## Decisions Made
- Phase detection logic refactored: position-zone check (standing/bottom) now takes priority over velocity direction
  - Reason: zone-based detection is more stable and less sensitive to noise at extremes
- Phase smoothing implemented at E3 level (not E2) — operates on discrete phase labels, not raw landmark data
- Two-pass windowing strategy adopted (`detect_refined_analysis_window`): rough crop first, then re-detect thresholds on cropped data for more precise rep boundaries

## Work Done
- `engine/squat/phases.py`:
  - `detect_squat_phase()` logic refactored (position zones → velocity direction)
  - `smooth_squat_phases()` — removes short-duration phase segments (hysteresis)
  - `detect_rough_movement_window()` — finds first DESCENDING to last ASCENDING
  - `detect_squat_analysis_window()` — rep-based `(start, end)` window
  - `detect_refined_analysis_window()` — two-pass refinement for precise crop
- `tests/test_squat_smooth_phases.py` — 5 unit tests for `smooth_squat_phases`
- `tests/test_squat_phase_smoothing_integration.py` — 2 integration tests (smoothing + rep detection)
- `tests/test_squat_pipeline_smoothed.py` — exploratory script for manual pipeline validation (not a pytest test)
- Docs updated: `ENGINE_PROGRESS.md`, `PROJECT_STATUS.md`, `CURRENT_SPRINT.md`

## Known Issues
- `phases.py` has a stray `from tracemalloc import start` import — needs removal
- `detect_squat_analysis_window()` return type annotation is `tuple[int, int]` but can return `None`

## Next Steps
- Remove stray `tracemalloc` import from `phases.py`
- Fix `detect_squat_analysis_window()` return type annotation (`Optional[tuple[int, int]]`)
- E3-B Core Metrics: squat depth, torso lean, knee travel, heel lift, neutral spine proxy

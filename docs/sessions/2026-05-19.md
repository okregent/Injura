# Session 2026-05-19

## Decisions Made
- `PoseFrame` retained as `List[Landmark]` type alias — no class conversion (ADR-010)
- Accessor strategy: standalone helper functions, not methods on `PoseLandmark` (ADR-011)
- Semantic separation: `landmarks.py` / `accessors.py` / `chains.py` as distinct modules (ADR-012)
- `feature/pose-foundation` and `feature/biomechanics` committed as separate branches

## Work Done
- `engine/pose/landmarks.py`: `PoseLandmark` IntEnum with named joint access (hips, knees, ankles, shoulders, elbows, wrists, heels, foot indices)
- `engine/pose/accessors.py`: `get_landmark(frame, landmark)`, `get_landmarks(frame, landmarks)` — removes raw index usage from analysis code
- `engine/pose/chains.py`: `LEFT_LEG_CHAIN`, `RIGHT_LEG_CHAIN`, `LEFT_ARM_CHAIN`, `RIGHT_ARM_CHAIN`, `TORSO_CHAIN`
- `engine/pose/__init__.py`: updated package exports
- `engine/biomechanics/vector.py`: `Vector2D`, `to_vector()`, `vector_length()`, `dot_product()`
- `engine/biomechanics/angles.py`: `calculate_angle(a, b, c)` using dot product / cosine rule, returns degrees
- `engine/biomechanics/visibility.py`: `is_visible()`, `all_visible()` — `all_visible` reuses `is_visible` per helper hierarchy principle
- Tests: `test_landmark_accessor.py`, `test_pose_package_exports.py`, `test_biomechanics_vector.py`, `test_biomechanics_angles.py`, `test_biomechanics_visibility.py`

## Next Steps
- Temporal smoothing layer (`engine/biomechanics/smoothing.py`) — moving average on raw landmarks
- Squat phase detection (`standing`, `descending`, `bottom`, `ascending`, `lockout`)
- Define `ExerciseAnalyzer` base class
- Implement `SquatAnalyzer`
